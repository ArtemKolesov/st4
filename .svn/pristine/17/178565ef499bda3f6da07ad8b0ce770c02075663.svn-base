package ua.nure.kolesov.SummaryTask4.db;
/**
 * Staff positions.
 *
 */
public enum Position {
	PILOT(1), NAVIGATOR(2), OPERATOR(3), HOSTESS(4);
	
	private int id;

	public int getId() {
		return id;
	}

	private Position(int id) {
		this.id = id;
	}
	
}
